The HPC was used for tasks that were impossible to do in a local machine.
I used it for the optimization of the control fields for the case where loss is considered and also to compute the dynamic speed limit (check section 5.2 of the thesis).

The toml files are from the HPC itself.

